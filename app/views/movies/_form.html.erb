<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie.errors.count, "error") %> encontrado al querer arrendar:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, 'Nombre Película', style: "display: block" %>
    <%= form.text_field :name, class:'form-control mb-4' %>
  </div>

  <div>
    <%= form.label :client_id, 'Nombre Cliente', class: 'form-label', style: 'display: block' %>    
    <%= form.select :client_id, options_for_select(Client.all.pluck(:name, :id), prompt: 'Selecciona un cliente'), {}, class: 'form-select' %>
  </div>

  <div>
    <%= form.submit 'Arrendar', class:'btn btn-success mt-4' %>
  </div>
<% end %>
